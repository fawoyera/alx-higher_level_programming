-- This script computes the score average of all records in the table "second_table" of the database "hbtn+0c_0" of MYSQL server
-- The database name should be passed as an argument of the mysql command
SELECT AVG(score) AS average FROM second_table;
